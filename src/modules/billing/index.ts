/**
 * Billing module (Phase 3) — plans, subscriptions, payments, auto-renew.
 * Gateways: bKash, Nagad, SSLCommerz (local) + Stripe/Paddle (international).
 * Emits `subscription.renewed` for the notification pipeline.
 *
 * Stubbed now so the folder boundary + event contract exist from day one.
 */
export {};
